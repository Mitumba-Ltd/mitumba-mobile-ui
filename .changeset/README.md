# Changesets

Every pull request that changes the public behavior or API of `@mitumba/mobile-ui` must include a Changeset.

Run:

```bash
npm run changeset
```

Select `@mitumba/mobile-ui`, choose the semantic version impact, and describe the consumer-visible change. Documentation, showcase-only, and CI-only changes do not require a Changeset.
